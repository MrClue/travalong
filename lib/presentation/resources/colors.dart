import 'package:flutter/material.dart';

class TravalongColors {
  // * 60-30-10 colors
  static const neutral_60 = Colors.white; // 60%
  static const primary_30 = Color.fromRGBO(245, 248, 253, 1.0); // 30%
  static const secondary_10 = Color.fromRGBO(42, 186, 255, 1.0); // 10%

  // * Additional
  static const placeholder_text = Color.fromRGBO(131, 131, 131, 1.0);
  static const primary_30_stroke = Color.fromRGBO(232, 240, 255, 1.0);

  // * Conditional colors
  static const primary_text_bright = Colors.black;
  static const primary_text_dark = Colors.white;

  static const secondary_text_bright = Color.fromRGBO(62, 62, 62, 1.0);
  static const secondary_text_dark = Color.fromRGBO(232, 240, 255, 1.0);
}
